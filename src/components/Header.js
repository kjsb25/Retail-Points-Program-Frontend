import { AppBar, Toolbar, Typography } from "@mui/material";

const HeaderComponent = () => {
  return (
    <AppBar>
      <Toolbar>
        <Typography variant="h5">Reward Points Dashboard</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default HeaderComponent;
